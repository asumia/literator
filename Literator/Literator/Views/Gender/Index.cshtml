@model GenderViewModel
@await Html.PartialAsync("filterBlock", new filterBlockViewModel { redirect = "", clientId = Model.ClientId })
<div class="navblock3">
    <nav class="nav2">
        <p class="dib baumans24-30 nnumber">Id</p>
        <p class="dib baumans24-30 nname">Name</p>
    </nav>
</div>
@{
    if (Model.genders != null)
    {
        if (Model.genders.Count() > 0)
        {
            int i = 0;
            while (i < Model.genders.Count())
            {
                <div class="book_info">
                    <ul class="edit_delete listnone nodecoration">
                        <li><a class="edit" asp-controller="Gender" asp-action="Edit" asp-route-ClientId=@Model.ClientId asp-route-id="@Model.genders.ElementAt(i).id"><img src="~/images/edit.svg" alt="edit" /></a></li>
                        <li><a class="delete" asp-controller="Gender" asp-action="Delete" asp-route-ClientId=@Model.ClientId asp-route-id="@Model.genders.ElementAt(i).id"><img src="~/images/delete.svg" alt="delete" /></a></li>
                    </ul>
                    <div class="number"><p class="id baumans">@Model.genders.ElementAt(i).id</p></div>
                    <div class="name"><p class="name_text baumans">@Model.genders.ElementAt(i).name</p></div>
                </div>
                i++;
            }
        }
    }
}
<div class="addBook_block">
    <a class="addBook_button baumans" asp-controller="Gender" asp-action="Add" asp-route-ClientId=@Model.ClientId>Add gender</a>
</div>